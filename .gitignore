node_modules/
.DS_Store
npm-debug.log*
dist/

# Icon-dex build scratch (regenerated by tools/build-icon-dex.js). The built
# assets/classes.dex IS committed; these intermediates are not.
r8.jar
temp_src/
temp_classes/

# Native virtual-camera binaries (built in CI, staged into assets/softcam/).
# The .gitkeep is tracked so the folder exists; the built binaries are not.
assets/softcam/*.dll
assets/softcam/*.exe
# Upstream softcam source, checked out into ./softcam during the CI build.
/softcam/
# softcam-bridge build intermediates.
native/softcam-bridge/*.obj
native/softcam-bridge/*.exe
